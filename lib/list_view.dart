import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class List extends StatelessWidget {
  const List({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Welcome"),
        centerTitle: true,
      ),
      drawer: const Drawer(),
      floatingActionButton: FloatingActionButton(
        child: const Icon(Icons.add),
        onPressed: () {},
      ),
      body: Padding(
        padding: const EdgeInsets.all(10.0),
        child: ListView(
          children: [
            ListTile(
                title: const Text('List Name1'),
                subtitle: const Text('You can do that'),
                leading: Container(
                  width: 60,
                    height: 60,
                    decoration: BoxDecoration(
                        color: Colors.blue,

                      borderRadius: BorderRadius.circular(30)
                    ),

                    child: const Center(
                      child: Text("1",
                          style: TextStyle(
                              fontSize: 40, fontWeight: FontWeight.bold)),
                    ))),
            const SizedBox(
              height: 10,
              width: double.infinity,
            ),

            ListTile(
                title: const Text('List Name1'),
                subtitle: const Text('You can do that'),
                leading: Container(
                    width: 60,
                    height: 60,
                    decoration: BoxDecoration(
                        color: Colors.blue,

                        borderRadius: BorderRadius.circular(30)
                    ),

                    child: const Center(
                      child: Text("1",
                          style: TextStyle(
                              fontSize: 40, fontWeight: FontWeight.bold)),
                    ))),
            const SizedBox(
              height: 10,
              width: double.infinity,
            ),

            ListTile(
                title: const Text('List Name1'),
                subtitle: const Text('You can do that'),
                leading: Container(
                    width: 60,
                    height: 60,
                    decoration: BoxDecoration(
                        color: Colors.blue,

                        borderRadius: BorderRadius.circular(30)
                    ),

                    child: const Center(
                      child: Text("1",
                          style: TextStyle(
                              fontSize: 40, fontWeight: FontWeight.bold)),
                    ))),
            const SizedBox(
              height: 10,
              width: double.infinity,
            ),

            ListTile(
                title: const Text('List Name1'),
                subtitle: const Text('You can do that'),
                leading: Container(
                    width: 60,
                    height: 60,
                    decoration: BoxDecoration(
                        color: Colors.blue,

                        borderRadius: BorderRadius.circular(30)
                    ),

                    child: const Center(
                      child: Text("1",
                          style: TextStyle(
                              fontSize: 40, fontWeight: FontWeight.bold)),
                    ))),
            const SizedBox(
              height: 10,
              width: double.infinity,
            ),

            ListTile(
                title: const Text('List Name1'),
                subtitle: const Text('You can do that'),
                leading: Container(
                    width: 60,
                    height: 60,
                    decoration: BoxDecoration(
                        color: Colors.blue,

                        borderRadius: BorderRadius.circular(30)
                    ),

                    child: const Center(
                      child: Text("1",
                          style: TextStyle(
                              fontSize: 40, fontWeight: FontWeight.bold)),
                    ))),
            const SizedBox(
              height: 10,
              width: double.infinity,
            ),

            ListTile(
                title: const Text('List Name1'),
                subtitle: const Text('You can do that'),
                leading: Container(
                    width: 60,
                    height: 60,
                    decoration: BoxDecoration(
                        color: Colors.blue,

                        borderRadius: BorderRadius.circular(30)
                    ),

                    child: const Center(
                      child: Text("1",
                          style: TextStyle(
                              fontSize: 40, fontWeight: FontWeight.bold)),
                    ))),
            const SizedBox(
              height: 10,
              width: double.infinity,
            ),

            ListTile(
                title: const Text('List Name1'),
                subtitle: const Text('You can do that'),
                leading: Container(
                    width: 60,
                    height: 60,
                    decoration: BoxDecoration(
                        color: Colors.blue,

                        borderRadius: BorderRadius.circular(30)
                    ),

                    child: const Center(
                      child: Text("1",
                          style: TextStyle(
                              fontSize: 40, fontWeight: FontWeight.bold)),
                    ))),
            const SizedBox(
              height: 10,
              width: double.infinity,
            ),


            ListTile(
                title: const Text('List Name1'),
                subtitle: const Text('You can do that'),
                leading: Container(
                    width: 60,
                    height: 60,
                    decoration: BoxDecoration(
                        color: Colors.blue,

                        borderRadius: BorderRadius.circular(30)
                    ),

                    child: const Center(
                      child: Text("1",
                          style: TextStyle(
                              fontSize: 40, fontWeight: FontWeight.bold)),
                    ))),
            const SizedBox(
              height: 10,
              width: double.infinity,
            ),
            ListTile(
                title: const Text('List Name1'),
                subtitle: const Text('You can do that'),
                leading: Container(
                    width: 60,
                    height: 60,
                    decoration: BoxDecoration(
                        color: Colors.blue,

                        borderRadius: BorderRadius.circular(30)
                    ),

                    child: const Center(
                      child: Text("1",
                          style: TextStyle(
                              fontSize: 40, fontWeight: FontWeight.bold)),
                    ))),
            const SizedBox(
              height: 10,
              width: double.infinity,
            ),
            ListTile(
                title: const Text('List Name1'),
                subtitle: const Text('You can do that'),
                leading: Container(
                    width: 60,
                    height: 60,
                    decoration: BoxDecoration(
                        color: Colors.blue,

                        borderRadius: BorderRadius.circular(30)
                    ),

                    child: const Center(
                      child: Text("1",
                          style: TextStyle(
                              fontSize: 40, fontWeight: FontWeight.bold)),
                    ))),
            const SizedBox(
              height: 10,
              width: double.infinity,
            ),
            ListTile(
                title: const Text('List Name1'),
                subtitle: const Text('You can do that'),
                leading: Container(
                    width: 60,
                    height: 60,
                    decoration: BoxDecoration(
                        color: Colors.blue,

                        borderRadius: BorderRadius.circular(30)
                    ),

                    child: const Center(
                      child: Text("1",
                          style: TextStyle(
                              fontSize: 40, fontWeight: FontWeight.bold)),
                    ))),
            const SizedBox(
              height: 10,
              width: double.infinity,
            ),
            ListTile(
                title: const Text('List Name1'),
                subtitle: const Text('You can do that'),
                leading: Container(
                    width: 60,
                    height: 60,
                    decoration: BoxDecoration(
                        color: Colors.blue,

                        borderRadius: BorderRadius.circular(30)
                    ),

                    child: const Center(
                      child: Text("1",
                          style: TextStyle(
                              fontSize: 40, fontWeight: FontWeight.bold)),
                    ))),
            const SizedBox(
              height: 10,
              width: double.infinity,
            ),     ListTile(
                title: const Text('List Name1'),
                subtitle: const Text('You can do that'),
                leading: Container(
                    width: 60,
                    height: 60,
                    decoration: BoxDecoration(
                        color: Colors.blue,

                        borderRadius: BorderRadius.circular(30)
                    ),

                    child: const Center(
                      child: Text("1",
                          style: TextStyle(
                              fontSize: 40, fontWeight: FontWeight.bold)),
                    ))),
            const SizedBox(
              height: 10,
              width: double.infinity,
            ),
            ListTile(
                title: const Text('List Name1'),
                subtitle: const Text('You can do that'),
                leading: Container(
                    width: 60,
                    height: 60,
                    decoration: BoxDecoration(
                        color: Colors.blue,

                        borderRadius: BorderRadius.circular(30)
                    ),

                    child: const Center(
                      child: Text("1",
                          style: TextStyle(
                              fontSize: 40, fontWeight: FontWeight.bold)),
                    ))),
            const SizedBox(
              height: 10,
              width: double.infinity,
            ),

          ],
        ),
      ),
    );
  }
}
